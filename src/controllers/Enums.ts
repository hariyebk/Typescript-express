export enum Methods{
    get =  "get",
    post =  "post",
    patch = "patch",
    put = "put",
    del = "delete"
}

export enum Metadata{
    path = "path",
    method = "method",
    middleware = "middleware",
    validator = "requiredProps"
}